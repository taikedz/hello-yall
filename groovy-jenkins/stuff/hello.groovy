def greet(name) {
    return "Hello, ${name.trim()}!"
}

// The whole file gets encapsulated to a class - each function becomes a method of it,
// and any "naked code" goes into an instance run() method.
// We must `return this;` so that the `load` operation receives the loaded instance.
return this;

